name: Discord Component Notification

on:
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master", "dev" ]
    types: [opened, closed]
  pull_request_review:
    types: [submitted]
  issue_comment:
    types: [created]
  create: # Branch or tag creation
  discussion_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  discord-notification:
    runs-on: ubuntu-latest
    if: github.event.issue.pull_request || github.event_name != 'issue_comment'
    steps:
      - name: Prepare Notification Details
        id: info
        run: |
          # --- IDマッピング関数 ---
          # 通常時用（逆転ロジック：actor自身ではなく相手にメンション）
          get_mention_reverse() {
            case "$1" in
              "tukuyomil032") echo "<@1100794879019323393>" ;;
              "zibasan")      echo "<@745902312345567252>" ;;
              *)              echo "@here" ;;
            esac
          }

          # マージ時用（正ロジック：actorをそのままメンション）
          get_mention_direct() {
            case "$1" in
              "tukuyomil032") echo "<@745902312345567252>" ;;
              "zibasan")      echo "<@1100794879019323393>" ;;
              *)              echo "@here" ;;
            esac
          }

          # 基本のメンション（操作した人・通常時は逆転ロジック）
          MENTION=$(get_mention_reverse "${{ github.actor }}")

          case "${{ github.event_name }}" in
            "push")
              TITLE="📦 ブランチ${{ github.ref_name }}にpushされました"
              DESC="コミット内容: ${{ github.event.head_commit.message }}"
              URL="${{ github.event.compare }}"
              COLOR=3066993
              BRANCH="${{ github.ref_name }}"
              ;;
            "pull_request")
              if [ "${{ github.event.action }}" = "closed" ] && [ "${{ github.event.pull_request.merged }}" = "true" ]; then
                TITLE=":white_check_mark: PRがMergeされました: #${{ github.event.pull_request.number }}"
                COLOR=6570404
                # マージ時は「マージした人」と「PR作成者」の両方にメンション
                AUTHOR_MENTION="@here"
                MENTION="$AUTHOR_MENTION"
              elif [ "${{ github.event.action }}" = "closed" ]; then
                TITLE=":x: PRがクローズされました: #${{ github.event.pull_request.number }}"
                COLOR=15158332
              else
                TITLE=":new: PRが作成されました: #${{ github.event.pull_request.number }}"
                COLOR=3066993
              fi
              DESC="${{ github.event.pull_request.title }}"
              BRANCH="${{ github.event.pull_request.base.ref }} <-- ${{ github.event.pull_request.head.ref }}"
              URL="${{ github.event.pull_request.html_url }}"
              ;;
            "pull_request_review")
              TITLE="🔍 レビュー: ${{ github.event.review.state }}"
              DESC="Pull Request: ${{ github.event.pull_request.title }}"
              URL="${{ github.event.review.html_url }}"
              BRANCH="${{ github.event.pull_request.base.ref }} <-- ${{ github.event.pull_request.head.ref }}"
              COLOR=15105570
              ;;
            "issue_comment")
              TITLE="💬 新しいコメント"
              DESC="${{ github.event.comment.body }}"
              URL="${{ github.event.comment.html_url }}"
              BRANCH="Pull Request #${{ github.event.issue.number }}"
              COLOR=12370112
              ;;
            "create")
              TITLE="🌿 ${{ github.event.ref_type }}の作成: ${{ github.event.ref }}"
              DESC="リポジトリ: ${{ github.repository }}"
              URL="https://github.com/${{ github.repository }}/tree/${{ github.event.ref }}"
              BRANCH="${{ github.event.ref }}"
              COLOR=3066993
              ;;
            "discussion_comment")
              TITLE="💬 Discussionコメントが作成されました"
              DESC="${{ github.event.comment.body }}"
              URL="${{ github.event.comment.html_url }}"
              BRANCH="Discussions"
              COLOR=12370112
              ;;
            "pull_request_review_comment")
              TITLE="📝 PRのコードレビューコメントが作成されました"
              DESC="${{ github.event.comment.body }}"
              URL="${{ github.event.comment.html_url }}"
              BRANCH="${{ github.event.pull_request.base.ref }} <-- ${{ github.event.pull_request.head.ref }}"
              COLOR=15105570
              ;;
          esac

          echo "title=$TITLE" >> $GITHUB_OUTPUT
          echo "desc=$DESC" >> $GITHUB_OUTPUT
          echo "url=$URL" >> $GITHUB_OUTPUT
          echo "color=$COLOR" >> $GITHUB_OUTPUT
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT
          echo "mention=$MENTION" >> $GITHUB_OUTPUT

      - name: Send Discord Webhook Embed
        shell: bash
        env:
          TITLE: ${{ steps.info.outputs.title }}
          DESC: ${{ steps.info.outputs.desc }}
          URL: ${{ steps.info.outputs.url }}
          COLOR: ${{ steps.info.outputs.color }}
          BRANCH: ${{ steps.info.outputs.branch }}
          MENTION: ${{ steps.info.outputs.mention }}
          SENDER: ${{ github.actor }}
        run: |
            # 1. 変数のエスケープとデフォルト値の設定
            TITLE_VAL="${TITLE:-No Title}"
            DESC_VAL="${DESC:-No Description}"
            COLOR_VAL="${COLOR:-3066993}"

            # 2. curlでJSONを送信 (componentsブロックを削除)
            curl -H "Content-Type: application/json" \
                  -X POST \
                  -d '{
                    "content": "'"${MENTION}"'",
                    "embeds": [{
                      "title": "'"${TITLE_VAL//\"/\\\"}"'",
                      "description": "'"${DESC_VAL//\"/\\\"}"'",
                      "color": '"${COLOR_VAL}"',
                      "url": "'"${URL}"'",
                      "author": {
                        "name": "'"${SENDER}"'",
                        "icon_url": "https://github.com/'"${SENDER}"'.png",
                        "url": "https://github.com/'"${SENDER}"'"
                      },
                      "fields": [
                        { "name": "👤 操作したユーザー", "value": "'"${SENDER}"'", "inline": true },
                        { "name": ":deciduous_tree: ブランチ", "value": "'"${BRANCH}"'", "inline": true }
                      ]
                    }]
                  }' \
                  ${{ secrets.DISCORD_WEBHOOK }}
